{
  "name": "server-hub-v7",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:client": "cd host-x && npm install && npm run build",
    "start": "cd api-server && node --enable-source-maps ./dist/index.mjs"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}